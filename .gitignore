# This repo serves static pages via github pages, so no python executables are necessary.
*.py
*.pyc

# OS X needs to shut its mouth
*.DS_Store

# You too, Windows!
*.ini

# Prevent redundant built pages from unnecessarily being added
source/site/*
